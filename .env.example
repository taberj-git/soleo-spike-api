# Server Configuration
PORT=3000

# HTTPS Configuration
# Set to 'true' to enable HTTPS, 'false' or omit for HTTP
USE_HTTPS=false

# SSL Certificate Paths (required when USE_HTTPS=true)
SSL_KEY_PATH=./certs/server.key
SSL_CERT_PATH=./certs/server.cert

# Logger Configuration
# Options: winston, default, test
LOGGER_TYPE=winston

# Authentication Configuration
# Options: azure
AUTH_TYPE=azure

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://localhost:5173
